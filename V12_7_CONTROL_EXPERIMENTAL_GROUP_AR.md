# V12.7 — Control / Experimental Group

أضيف في هذه النسخة خيار بحثي اختياري لتقسيم الطلبة إلى مجموعتين:

- `experimental`: تستخدم الدروس، المحاكيات، AI Coach، و Concept Builder.
- `control`: تستخدم الدروس، المحاكيات، التأملات، والاختبارات دون أدوات الذكاء الاصطناعي.

الإعداد الافتراضي آمن ولا يغيّر الدراسة الحالية:

```toml
ENABLE_CONTROL_GROUP = "false"
```

عند تفعيله في Streamlit secrets، يتم توزيع الطلبة الجدد بالتوازن بين المجموعتين، وتظهر قيمة `study_group` في لوحة المقيّم والتصدير البحثي.

ملاحظات بحثية مهمة:

1. لا تفعلي هذا الخيار وسط تجربة بدأت فعليًا إلا إذا كان ذلك متوافقًا مع بروتوكولك البحثي وموافقة المشاركين.
2. الطلبة الموجودون سابقًا لا تُحذف بياناتهم ولا يُعاد ضبط نتائجهم.
3. مجموعة الضبط لا تُجبر على استخدام AI قبل الاختبار البعدي.
4. مجموعة التجربة تبقى مطالبة بتفاعل AI واحد على الأقل قبل الاختبار البعدي.

# 3alimnIA V5 - الواجهة الاحترافية والملاحة الأصلية

## لماذا أُعيد بناء التنقل؟

الإصدارات السابقة اعتمدت على أزرار مخصصة وحيل CSS/JavaScript لفتح القائمة الجانبية أو إبقاء شريط النجاة ظاهرًا. هذا جعل السلوك حساسًا لتغييرات DOM في Streamlit Cloud، ونتج عنه أحيانًا أزرار لا تستجيب أو مستخدم عالق داخل صفحة.

في V5 تم استبدال هذه الطبقة بالكامل بالملاحة الرسمية في Streamlit:

- `st.Page`
- `st.navigation(position="top")`
- `st.switch_page`

وبذلك أصبحت الوجهات مسجلة فعليًا كصفحات، وليست مجرد قيم نصية في `session_state`.

## ما تغير بصريًا ووظيفيًا؟

### 1. شريط تنقل علوي أصلي

- أقسام واضحة للمتعلم: نظرة عامة، التعلم، التقييم، البحث، الحساب.
- أقسام واضحة للمقيّم: نظرة عامة، المتعلمون، الذكاء الاصطناعي والجودة، البيانات والتصدير، الحساب.
- تمييز الصفحة النشطة تلقائيًا.
- عرض الأقسام كقوائم منسدلة عند ضيق المساحة.
- اختفاء مشكلة طول القائمة الجانبية نهائيًا.

### 2. شريط حساب دائم

يظهر في كل صفحة داخلية ويحتوي على:

- اللغة.
- الرئيسية.
- تغيير الحساب.
- تغيير الفضاء.
- تسجيل الخروج.

تعمل هذه الأوامر عبر callbacks أصلية، ولا تعتمد على النقر البرمجي داخل DOM.

### 3. توجيه ديناميكي حسب الصلاحيات

- لا تظهر للمتعلم إلا الصفحات المسموح بها في مرحلته الحالية.
- بعد الموافقة أو الاختبار أو إكمال الوحدة، تُضاف الصفحة التالية تلقائيًا.
- قبل دخول المقيّم لا تظهر أدوات الإدارة.
- بعد تسجيل الدخول تظهر أقسام المقيّم كاملة، ومنها التصدير.

### 4. توافق أفضل مع اللغات الثلاث

- العربية RTL.
- الفرنسية والإنجليزية LTR.
- عناوين الأقسام والصفحات مترجمة في الملاحة نفسها.
- تبقى أسماء Qiskit والدوال البرمجية كما هي.

### 5. تصميم أكثر قربًا من منصات التعلم الحديثة

- واجهة أولية واضحة بدل تكديس أدوات كثيرة في عمود طويل.
- تدرج من المسار إلى الدرس ثم التقييم.
- صفحة نشطة ظاهرة بوضوح.
- أدوات الحساب مستقلة عن محتوى الدرس.
- بطاقات تقدم وجداول وتبويبات أكثر كثافة ووضوحًا.

## ملفات V5 الأساسية

- `app.py`: موجه الصفحات الأصلي والإطار المشترك.
- `router.py`: طابور التنقل الآمن بين الصفحات.
- `main_app.py`: المحتوى التعليمي والبحثي مع ربطه بالموجه الجديد.
- `.streamlit/v4_theme.css`: تصميم الملاحة والشريط العلوي.
- `requirements.txt`: تثبيت Streamlit 1.53 أو أحدث ضمن السلسلة 1.x.
- `validate_v5_native_router.py`: فحص بنيوي قبل الرفع.

## ملاحظة مهمة عند الرفع

انسخ ملفات V5 فوق المستودع مع الحفاظ على `.git` وملف الأسرار الحقيقي. بعد رفعها، يجب أن يعيد Streamlit Cloud تثبيت المتطلبات لأن V5 يعتمد على الملاحة العلوية المتاحة في الإصدارات الحديثة من Streamlit.

# تدقيق مشروع 3alimnIA بعد V6.19.1 وخطة التطوير

## الخلاصة التنفيذية

المشروع لم يعد نموذج Streamlit بسيطًا. بنيته الحالية تحتوي على خطوط إنتاج تربوية، بحث ويب، تركيب أدلة، مخطط مقرر قابل للإصدار، توليد على مستوى أجزاء الدرس، بوابة جودة تربوية، نمذجة أدلة المتعلم، دعم تكيفي، مساحة باحث/مقيّم، وتعدد لغات RTL/LTR. لذلك أكبر خطر لم يعد «نقص الميزات»، بل انفصال بعض هذه الطبقات عن بعضها وتزايد التعقيد داخل ملفات مركزية ضخمة.

أهم فجوة وظيفية وجدت بعد V6.19.1 كانت أن خط إنتاج الأستاذ المتقدم ينتهي بمحتوى منظم ومعتمد، بينما المتعلم في `Published Courses` كان يرى معاينة قديمة لمخرجات مراحل legacy. لذلك اختير V6.20 لمعالجة حلقة القيمة كاملة: **الأستاذ يبني ويعتمد → ينشر → المتعلم يتعلم من النسخة المعتمدة → يحاول قبل الذكاء الاصطناعي → يحصل على دعم تكيفي → يحفظ تقدمه → يرى الأستاذ مؤشرات مجمعة**.

## نقاط القوة الحالية

1. **حوكمة تربوية جيدة نسبيًا**: توجد موافقات بشرية على evidence والـblueprint وأجزاء الدرس، مع بوابة جودة منفصلة عن LLM المنتج للمحتوى.
2. **قابلية تدقيق جيدة**: توجد سجلات للإصدارات والمراجعات والموافقات وسجل تفاعلات AI.
3. **فصل معقول بين البحث والتعليم**: مسار دراسة Qiskit يحمل قيودًا بحثية واضحة، وV6.20 حافظ على بيانات المقررات العامة في جداول تشغيلية منفصلة.
4. **تعدد اللغات والاتجاه**: العربية والفرنسية والإنجليزية أصبحت جزءًا بنيويًا من الواجهة وليست طبقة ترجمة سطحية فقط.
5. **تدرج تربوي واضح**: محاولة المتعلم قبل AI، دعم متدرج، تأمل، ومؤشرات أدلة بدل ادعاءات mastery غير مبررة.
6. **تراكم اختبارات تعاقدية كبير**: رغم قدم بعضها، هناك رصيد مهم من اختبارات الانحدار يمنع فقدان ميزات سابقة بسهولة.

## المخاطر ذات الأولوية

### أولوية P0 — تم التعامل معها في V6.20

- **تسرب ملف Secrets داخل ZIP**: الملف المرفوع كان يحتوي `.streamlit/secrets.toml`. هذا خطر تسليم مباشر حتى لو كان Git يتجاهله. الحل: حذف الملف من الإصدار المسلّم وإضافة packager آمن. يوصى بتدوير أي اعتماد حقيقي سبق إدراجه في تلك الحزمة.
- **فجوة النشر/التشغيل**: المقرر المعتمد لم يكن هو ما يراه المتعلم. الحل: runtime مبني على approved blueprint + approved lesson blocks.
- **مسار Published Courses غير مسجل في الموجه الأصلي**: أصلح في `app.py`.
- **خطر تعميم مدرب Qiskit على مجالات أخرى**: أضيف عقد coach عام مستقل للمقررات المنشورة.

### أولوية P1 — ينبغي أن تكون محور V6.21–V6.22

- **الملفات المركزية ضخمة**: `main_app.py` يقارب 5.8k سطر، `teacher_studio.py` أكثر من 3.6k، و`db.py` يقارب 4k. هذا يزيد coupling ويجعل الإصلاحات مكلفة وعرضة للانحدار.
- **صفحات `pages/student` و`pages/evaluator` ما تزال placeholders**: مشروع التقسيم بدأ ولم يكتمل.
- **إدارة schema تراكمية داخل `init_db()`**: مناسبة للنموذج الأولي لكنها ليست بديلًا طويل المدى عن migrations قابلة للترقية/الرجوع/التدقيق.
- **الاختبارات التاريخية مرتبطة أحيانًا بنصوص إصدار أو CSS بعينه**: نحتاج فصل اختبارات السلوك الحالية عن اختبارات الأرشيف البصري القديمة. أضيف `validate_current_release.py` كبداية.
- **التقييم العام للمقررات ما يزال محتوى نصيًا**: لا توجد بعد طبقة generic assessment items/results بنفس نضج دراسة Qiskit.
- **التحليلات التشغيلية للأستاذ أولية**: V6.20 يقدم aggregates فقط عمدًا. نحتاج لاحقًا analytics تربوية تحافظ على الخصوصية وتوضح التسرب/التعثر بدون كشف محادثات خام.

### أولوية P2 — قبل التوسع المؤسسي

- RBAC أدق وتدقيق صلاحيات على مستوى المؤسسة/المقرر.
- سياسات احتفاظ وحذف وتصدير بيانات واضحة.
- Observability: traces، error budgets، provider latency/cost، queue health.
- E2E browser tests واختبارات accessibility آلية ويدوية.
- فصل طبقة مزودي LLM عن Streamlit بصورة أكبر لدعم jobs والخدمات الخلفية.

## القرار المعماري المقترح

لا أوصي بإعادة كتابة المنصة من الصفر. الأفضل اتباع **Strangler Refactor** تدريجي:

1. كل ميزة جديدة توضع في module مستقل كما حدث في `published_course_runtime.py`.
2. تبقى الدوال القديمة كواجهات توافق مؤقتة.
3. ننقل عمليات DB الخاصة بكل bounded context إلى repositories تدريجيًا.
4. ننقل rendering من `main_app.py` إلى صفحات فعلية واحدة بعد أخرى مع اختبار قبل/بعد.
5. بعد استقرار boundaries، ندخل migrations رسمية وطبقة service واضحة.

هذا أقل مخاطرة من إعادة بناء شاملة قد تفقد أشهرًا من العمل التربوي والبحثي المتراكم.

## ما أنجز في دورة التطوير الحالية

- رفع الإصدار إلى `v6.20.0-published-course-runtime`.
- إضافة runtime للمقررات المنشورة.
- إضافة ثلاث جداول تشغيلية للمقرر.
- إضافة publication readiness gate حقيقي.
- تثبيت blueprint version لكل متعلم عند التسجيل.
- إضافة attempt/reflection/progress persistence.
- إعادة استخدام adaptive evidence مع حدود تفسيرية محافظة.
- إضافة coach عام grounded في محتوى الأستاذ المعتمد.
- إصلاح native routing لمقررات الأساتذة.
- إضافة مؤشرات مجمعة للأستاذ.
- منع التخطي إلى درس مستقبلي غير مفتوح.
- حماية حالة المقرر المكتمل من الرجوع إلى active بمجرد التنقل.
- تنظيف ملفات repository العرضية القديمة.
- حذف Secrets من حزمة التسليم وإضافة release packager.
- إضافة اختبار V6.20 واختبار current-release شامل.

## الخطوة العلمية/المنتجية التالية المقترحة

أوصي بأن يكون **V6.21 = Structured Assessment Runtime** بدل إضافة مؤثرات بصرية جديدة. الهدف هو تحويل `formative_assessment` وassessment alignments الموجودة في الـblueprint إلى عناصر تقييم منظمة لها item id، outcome mapping، rubric/answer contract، attempt، feedback، وevidence event. عندها يصبح لدينا حلقة تعليمية قابلة للقياس من المصدر إلى النتيجة، بدل الاعتماد على النصوص الحرة وحدها.

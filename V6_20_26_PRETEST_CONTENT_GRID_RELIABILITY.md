# V6.20.26 — Pre-test content + grid reliability

- Prevents placeholder concepts such as `$Untitled`, `Untitled`, `TBD`, and equivalent empty labels from reaching learner-facing fallback diagnostics.
- Applies the desktop 2x2 option grid at pre-test form scope so it is no longer dependent on Streamlit border-wrapper DOM details.
- Keeps mobile choices in a single column.
- Does not change authentication, enrollment, scoring, persistence, or course routing.


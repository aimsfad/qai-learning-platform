# V6.20.27.3 — إصلاح ميزانية طلب الاختبار القبلي الذكي

## المشكلة
كان ضاغط طلبات Groq مصممًا أساسًا لطلبات Teacher Studio التي تحتوي على أقسام `# Phase` و`<teacher_project_brief>`. طلبات الاختبار القبلي الذكي تستخدم عقد JSON مختلفًا، ولذلك عند تجاوز الميزانية كان الضاغط لا يجد الأقسام ويعيد نصًا فارغًا تقريبًا. ظهر ذلك في التشخيص بالشكل `~11708 -> ~0 input tokens` ثم `question_count=0`.

## الإصلاح
أضيف مسار احتياطي عام للطلبات غير المهيكلة يحفظ بداية الطلب (التعليمات وعقد JSON) ونهايته (سياق المقرر والأدلة) ضمن ميزانية الرموز. لا يسمح هذا المسار بإرسال طلب فارغ إلى أي مزود.

## النطاق
لا تغيير على Enrollment أو درجات الاختبار أو قاعدة البيانات أو واجهة المتعلم. التعديل محصور في موثوقية ضغط طلبات LLM والتحقق الآلي منها.

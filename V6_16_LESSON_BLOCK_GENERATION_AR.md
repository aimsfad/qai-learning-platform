# 3alimnIA V6.16 - توليد محتوى الدرس على مستوى الكتل

## الهدف
ينقل هذا الإصدار المنصة من توليد درس طويل في استجابة واحدة إلى توليد وحدات تعليمية صغيرة قابلة للمراجعة والاستبدال بصورة مستقلة.

## الكتل المدعومة
1. تنشيط المعارف السابقة.
2. شرح المفهوم.
3. مثال محلول.
4. تدريب موجه.
5. تدريب مستقل.
6. الأخطاء الشائعة ومعالجتها.
7. تقويم تكويني.
8. ملخص الدرس.
9. موارد ومتابعة.

## سير العمل
مخطط مقرر معتمد -> اختيار الدرس -> اختيار الكتلة -> بناء برومبت مقيد بالمخطط -> استدعاء مزود LLM -> التحقق من الاستشهادات والبنية -> حفظ إصدار غير قابل للكتابة فوقه -> مراجعة الأستاذ -> اعتماد الكتلة -> احتساب تقدم اكتمال الدرس.

## الملفات الجديدة أو المعدلة
- `lesson_block_generation_engine.py`: بناء البرومبت، التوليد، التحقق، الحفظ، والتحرير.
- `db.py`: جداول كتل الدروس، الإصدارات، الاعتماد، وسجل التدقيق.
- `teacher_studio.py`: مساحة بناء الدروس على مستوى الكتل.
- `validate_v616_lesson_block_generation.py`: اختبار سلوكي محلي.
- `.streamlit/secrets_example.toml`: مفاتيح التفعيل.

## قاعدة البيانات
أضيف جدولان:
- `teacher_lesson_block_runs`
- `teacher_lesson_block_audit_events`

يحفظ كل إصدار: المشروع، مخطط المقرر، الدرس، نوع الكتلة، البرومبت، المحتوى، المزود، النموذج، حالة التحقق، زمن الاستجابة، الإصدار الأب، رقم الإصدار، نوع المراجعة، اعتماد الأستاذ، وتاريخ الإنشاء.

## بوابة الجودة
يفحص النظام:
- عدم فراغ المحتوى.
- الحد الأدنى التقريبي للطول.
- عدم حفظ رسالة فشل مزود كمحتوى تعليمي.
- عدم استعمال مصدر غير موجود في مخطط الدرس.
- وجود بنية Markdown قابلة للعرض.

تكون النتيجة `completed` أو `needs_review` أو `error`.

## إعداد Streamlit Secrets
```toml
ENABLE_LESSON_BLOCK_GENERATION = "true"
REQUIRE_BLOCK_APPROVAL_FOR_LESSON_COMPLETION = "true"
```

## أوامر الاختبار
```cmd
py -3 validate_v616_lesson_block_generation.py
py -3 validate_v615_blueprint_editor_versioning.py
py -3 validate_v614_lesson_blueprint.py
py -3 validate_v6131_provider_quota_resilience.py
py -3 validate_v613_evidence_synthesis.py
py -3 validate_v612_research_augmented_builder.py
py -3 -m compileall .
```

## القيمة البحثية
يسمح V6.16 بقياس الجودة والزمن والتعديلات البشرية على مستوى مكون الدرس، بدل قياس استجابة طويلة واحدة. ويمكن مقارنة أنواع الكتل من حيث معدل القبول، حجم التعديل، زمن التوليد، صحة الاستشهادات، والتناسق مع مخطط المقرر.

## حدود الإصدار
- لا يدمج الكتل المعتمدة تلقائيًا في صفحة درس نهائية للمتعلم؛ هذه خطوة V6.17.
- لا ينفذ اختبارًا تربويًا ميدانيًا؛ الاختبارات الحالية برمجية وسلوكية محلية.
- جودة المحتوى تعتمد على المزوّد والمصادر ومراجعة الأستاذ.

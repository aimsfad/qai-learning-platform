# 3alimnIA V6.17 — خط الإنتاج الخلفي الهجين

## الهدف
ينقل هذا الإصدار التوليد الطويل من تنفيذ مباشر داخل دورة Streamlit إلى بنية مهام قابلة للاستمرار، مع حفظ كل مهمة في قاعدة البيانات، ودعم RQ/Redis للتنفيذ الخلفي الحقيقي.

## المخطط

- المراحل 01–03: متسلسلة مع اعتماد الأستاذ.
- المراحل 04–09: دفعة إنتاج متوازية بعد اكتمال المرحلة 03.
- المرحلة 10: تنتظر اكتمال 04–09.
- المرحلة 11: تنتظر اكتمال المرحلة 10.

## المكونات

- `production_pipeline.py`: تعريف DAG، إنشاء الوظائف، التبعيات، التنفيذ، retries الآمنة.
- `production_worker.py`: عامل RQ مستقل.
- جدول `teacher_production_jobs`: سجل دائم للحالة والتقدم والنتيجة والأخطاء الآمنة.
- واجهة خريطة الإنتاج: تعرض حالات queued/running/waiting/failed وتوفر زر «بدء الإنتاج الشامل».

## أوضاع التشغيل

### RQ/Redis — موصى به للإنتاج

```toml
PRODUCTION_QUEUE_BACKEND = "rq"
REDIS_URL = "redis://host:6379/0"
PRODUCTION_QUEUE_NAME = "3alimnia-production"
```

تشغيل العامل:

```bash
python production_worker.py
```

يجب تشغيل العامل كخدمة مستقلة عن Streamlit، وتمرير مفاتيح LLM وDATABASE_URL إليه كمتغيرات بيئة.

### Inline — للتطوير والاختبار فقط

```toml
PRODUCTION_QUEUE_BACKEND = "inline"
```

هذا الوضع يحفظ الوظائف في قاعدة البيانات لكنه ينفذها داخل عملية Streamlit بصورة متزامنة، لذلك لا يوفر خلفية دائمة.

## حالات الوظائف

- `queued`
- `waiting_for_dependency`
- `running`
- `generated`
- `needs_review`
- `failed`
- `retrying`
- `canceled`

## الخصوصية والأمان

- لا تُخزن مفاتيح API داخل جدول الوظائف.
- تعرض الواجهة رسالة خطأ آمنة، بينما يبقى التشخيص الكامل في سجل العامل.
- يتحقق العامل من ملكية المشروع قبل التنفيذ.
- لا تبدأ أي مرحلة قبل اكتمال تبعياتها.

## القيود

Streamlit Community Cloud لا يشغل عامل RQ دائمًا داخل التطبيق نفسه. يلزم نشر Redis وعامل منفصل عبر خدمة تدعم العمليات طويلة العمر، مثل Render أو Railway أو Fly.io أو خادم مؤسسي.

# 3alimnIA V6.11 — محرك بناء المحتوى التعليمي

تُحوّل هذه النسخة **Teacher Content Studio** من واجهة تجمع بيانات المشروع وتعرض برومبتًا إلى خط إنتاج فعلي يبني المقرر التعليمي مرحلةً بعد مرحلة، مع حفظ النتائج، والتحقق منها، وإتاحة مراجعة الأستاذ.

## المسار التنفيذي

1. يحفظ الأستاذ تعريف المشروع والمراجع وطريقة التدريس والتقييم.
2. تبني المنصة برومبتًا خاصًا بالمرحلة الحالية فقط، بدل إرسال تعليمات المراحل الإحدى عشرة كاملة في كل طلب.
3. تُضمَّن مخرجات المراحل المكتملة السابقة بوصفها سياقًا معتمدًا لضمان الاتساق.
4. يستدعي المحرك النموذج الأساسي ثم مزودي fallback المتاحين عند الفشل.
5. يخضع الناتج لفحص بنيوي أولي.
6. يُحفظ كل طلب ونتيجة وتشخيص وزمن استجابة في قاعدة البيانات.
7. عند نجاح المرحلة ينتقل المشروع تلقائيًا إلى المرحلة التالية.
8. يستطيع الأستاذ تعديل أحدث ناتج وحفظه بوصفه مراجعة بشرية معتمدة.

## الإصلاح الجوهري في حساب التقدم

كان عنوان المشروع يعتمد أحدث محاولة لكل مرحلة، حتى لو كانت محاولة فاشلة بعد نتيجة ناجحة. لذلك كان من الممكن أن يظهر المشروع `1/11` في بطاقة المشاريع ثم يعود إلى `0/11` داخل مساحة المشروع. أصبحت المنصة الآن تفضّل أحدث نتيجة مكتملة عند حساب التقدم والمعاينة، مع الاحتفاظ بالمحاولة الفاشلة في السجل التشخيصي.

## التحسينات التقنية

- إضافة `educational_builder.py` لتجميع منطق البرومبت والتوليد والتحقق والحفظ.
- تقليص البرومبت إلى المرحلة المطلوبة فقط لتقليل زمن الاستجابة وتسرب تعليمات المراحل الأخرى.
- وضع حدود لطول محتوى المصادر والسياق السابق.
- حماية البرومبت من التعليمات المضمّنة داخل الملفات المرفوعة.
- تمرير مخرجات المراحل السابقة إلى المرحلة الجديدة بصورة مضبوطة.
- إضافة retries محدودة للأخطاء المؤقتة وRate Limits.
- إضافة زمن الاتصال وزمن القراءة بصورة منفصلة.
- حفظ `latency_ms`، وحالة التحقق، واستعمال fallback.
- إضافة حالة `needs_review` بدل اعتبار الناتج القصير أو غير المنظم ناجحًا تلقائيًا.
- إضافة محرر لمراجعة الأستاذ واعتماد المرحلة.
- إضافة بحث ويب اختياري عبر Groq GPT-OSS للمراحل الحساسة للأدلة 1 و11.

## إعدادات Streamlit الجديدة

```toml
CONTENT_CONNECT_TIMEOUT_SECONDS = "15"
CONTENT_GENERATION_TIMEOUT_SECONDS = "180"
CONTENT_GENERATION_RETRIES = "2"

# اختياري وقد يضيف تكلفة وزمنًا إضافيًا
ENABLE_CONTENT_WEB_RESEARCH = "false"
CONTENT_REASONING_EFFORT = "low"
```

عند تفعيل البحث الويبّي، يستخدمه المحرك فقط في:

- المرحلة 1: تدقيق الأدلة والمفهوم.
- المرحلة 11: ضمان الجودة.

## الاختبار

```bash
python validate_v611_content_builder.py
```

يغطي الاختبار:

- عزل تعليمات المرحلة المختارة.
- تمرير سياق المرحلة السابقة.
- عدم ضياع التقدم بعد محاولة توليد فاشلة.
- حفظ القياسات التشخيصية.
- الانتقال التلقائي إلى المرحلة التالية.

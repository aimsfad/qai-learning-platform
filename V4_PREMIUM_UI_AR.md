# 3alimnIA V4 - تحسين بصري شامل وخطوط احترافية

## ما الذي تغير؟

أُعيد تنظيم واجهة المنصة بصريًا مع الحفاظ على منطق الدراسة، قاعدة البيانات، المسار التعليمي، AI Coach، Concept Builder، الاختبارات، ولوحات المقيّم.

### 1. الصفحة الرئيسية
- استعمال الشعار الرسمي المعتمد بواسطة `st.image` لمنع ظهور صورة مكسورة على Streamlit Cloud.
- Hero حديث بخلفية خفيفة، تسلسل بصري واضح، وأزرار دعوة إلى الإجراء.
- رسم بصري منظم لمسارات Quantum وML وAI.
- بطاقات مسارات متناسقة، مع حالات Available وComing soon.
- قسم مستقل يشرح منهجية التعلم وقيمة LPQS للجامعات والباحثين.

### 2. الخطوط واللغات
- العربية: Alexandria للعناوين وTajawal للنصوص، مع بدائل آمنة عند تعذر تحميل Google Fonts.
- الإنجليزية والفرنسية: Inter مع بدائل آمنة.
- دعم RTL للعربية وLTR للفرنسية والإنجليزية.
- تحسين line-height، المحاذاة، أحجام العناوين، وتباعد الفقرات في كل لغة.

### 3. لوحة التنقل
- شعار واضح لا يعتمد على HTML image data URI.
- لوحة زجاجية خفيفة وثابتة أثناء التمرير على الشاشات الواسعة.
- بطاقة ملف المتعلم، شريط التقدم، الخطوة التالية، والتنقل النشط.
- تحسين أزرار المتعلم والمقيّم مع حالة نشطة واضحة.

### 4. جميع الصفحات الداخلية
- Header موحد وجذاب لكل صفحة.
- توحيد البطاقات، النماذج، حقول الإدخال، Tabs، Expanders، Metrics، الجداول والتنبيهات.
- تحسين كتل الكود والمحاكيات دون تغيير اتجاه الشيفرة البرمجية في العربية.
- تصميم responsive للشاشات الصغيرة والمتوسطة.

## الملفات الجديدة أو المعدلة

- `.streamlit/v4_theme.css`: نظام التصميم الجديد ويُحمّل بعد CSS القديم لضمان أولوية القواعد.
- `.streamlit/config.toml`: ألوان Streamlit الأساسية وإيقاف جمع إحصاءات الاستخدام.
- `.gitattributes`: توحيد نهايات الأسطر وتقليل تحذيرات LF/CRLF على Windows.
- `branding.py`: مكونات Hero والأقسام والبطاقات متعددة اللغات.
- `main_app.py`: شعار موثوق، Sidebar جديد، Landing page جديدة، وHeaders موحدة.
- `config.py`: تحميل V4 CSS بعد stylesheet السابق.
- `i18n.py`: خطوط واتجاه ومحاذاة خاصة بكل لغة.
- `validate_v4_ui.py`: فحص سريع للأصول والملفات والتكامل البصري.

## تشغيل المنصة

```bash
pip install -r requirements.txt
streamlit run app.py
```

## الرفع إلى الفرع الحالي

بعد نسخ محتويات هذه النسخة إلى مجلد المستودع مع الإبقاء على `.git`:

```bash
git status
git add -A
git commit -m "Add V4 premium multilingual UI and typography"
git pull --rebase origin ux-improvements-v3-9
git push origin ux-improvements-v3-9
```

## فحوص الجودة المنفذة

```bash
python -m compileall .
python validate_multilingual.py
python validate_v4_ui.py
```

ملاحظة: الخطوط الخارجية تُحمّل من Google Fonts، مع وجود خطوط بديلة من النظام إذا تعذر الاتصال.

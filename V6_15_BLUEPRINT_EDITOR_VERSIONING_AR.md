# 3alimnIA V6.15 - محرر مخطط المقرر وإدارة الإصدارات

## 1. الهدف

يحوّل هذا الإصدار مخطط V6.14 من مخرج قابل للمراجعة فقط إلى كائن تخطيطي يمكن للأستاذ تحريره فعليًا دون فقدان أي نسخة سابقة. يدعم النظام إضافة العناصر وحذفها وإعادة ترتيبها، ثم يعيد التحقق من سلامة الروابط والمحاذاة قبل اعتماد النسخة الجديدة.

## 2. ما أضيف

- محرر جدولي ديناميكي للمفاهيم والوحدات والدروس وأهداف التعلم.
- إضافة وحذف صفوف مع توليد معرفات ثابتة للعناصر الجديدة.
- تعديل ترتيب الوحدات والدروس بواسطة `sequence_order`.
- تعديل المتطلبات السابقة والمصادر والمدة والتصورات الخاطئة.
- إعادة بناء روابط الهدف والنشاط والتقويم تلقائيًا.
- حفظ كل تعديل كنسخة جديدة لها `parent_run_id` ورقم مراجعة.
- سجل تدقيق على مستوى المخطط والعناصر المضافة أو المحذوفة أو المعدلة.
- استعادة أي نسخة تاريخية على شكل مسودة جديدة.
- عدم تعديل النسخة المعتمدة في مكانها؛ كل تحرير يحتاج اعتمادًا جديدًا.

## 3. سير العمل

```text
مخطط مولد أو معتمد
        ↓
فتح تبويب تحرير المخطط
        ↓
تعديل الجداول وإعادة الترتيب
        ↓
إدخال ملخص التعديل
        ↓
التطبيع والتحقق من المراجع
        ↓
إعادة حساب بوابة الجودة
        ↓
حفظ Revision جديد غير معتمد
        ↓
مراجعة الأستاذ واعتماد النسخة
```

## 4. بنية البيانات

أضيفت الحقول التالية إلى `teacher_blueprint_runs`:

- `parent_run_id`
- `revision_number`
- `change_summary`
- `edited_by`
- `edit_source`

كما أضيف جدول `teacher_blueprint_change_log` لتسجيل نوع العملية والعنصر قبل التغيير وبعده. وأضيف `sequence_order` إلى جدول الدروس.

## 5. قواعد السلامة

عند الحفظ تقوم المنصة بما يلي:

1. إزالة الصفوف الفارغة والتكرارات غير المقصودة.
2. تخصيص معرفات للعناصر الجديدة مع الحفاظ على المعرفات القائمة.
3. التحقق من أن كل درس ينتمي إلى وحدة موجودة.
4. حذف الإشارات إلى مفاهيم محذوفة من الدروس.
5. التحقق من أن كل هدف ينتمي إلى درس موجود.
6. إنشاء معرف نشاط وتقويم عند غيابهما.
7. إعادة بناء قوائم الدروس والمفاهيم والمصادر على مستوى الوحدة.
8. فحص دورات المتطلبات السابقة والمفاهيم اليتيمة.
9. إعادة حساب التغطية والمحاذاة وقابلية تتبع المصادر ودرجة الجاهزية.

## 6. الإعدادات

```toml
ENABLE_BLUEPRINT_EDITOR = "true"
BLUEPRINT_MAX_REVISION_HISTORY = "50"
```

## 7. التثبيت والاختبار

```cmd
py -3 validate_v615_blueprint_editor.py
py -3 validate_v614_lesson_blueprint.py
py -3 -m compileall .
```

## 8. حدود الإصدار

المحرر جدولي ومقصود به الضبط الدقيق والآمن. السحب والإفلات البصري لعقد خريطة المفاهيم، والمقارنة المرئية جنبًا إلى جنب بين نسختين، يمكن تطويرهما في إصدار لاحق.

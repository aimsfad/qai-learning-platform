# V8.7 Layout Fix

- Fixed the in-page left navigation shell by using a real Streamlit bordered container instead of an HTML wrapper around widgets.
- Removed the empty white block that appeared above the sidebar content on Streamlit Cloud.
- Restored readable card styling for student dashboard blocks, chips, and dashboard tiles.
- Kept the native Streamlit sidebar hidden to avoid duplicate sidebars.
- No database, authentication, feedback engine, or content changes.

# V6.20.27.4 — Structured AI pre-test output

## المشكلة
بعد إصلاح ميزانية Prompt في V6.20.27.3 أصبح Groq يستقبل الطلب فعلًا، لكن استجابة ناجحة من المزود قد تبقى نصًا حرًا أو JSON غير قابل للاستخراج. عندها يرى محرك الاختبار `question_count=0` رغم أن الاتصال بالنموذج نجح.

## الإصلاح
- يمرر محرك الاختبار القبلي JSON Schema صريحًا إلى طبقة التوليد في المحاولات الثلاث.
- عند استخدام Groq مع `openai/gpt-oss-20b` أو `openai/gpt-oss-120b` تستخدم المنصة Structured Outputs مع `strict: true`.
- يفرض المخطط ستة أسئلة، أربعة خيارات لكل سؤال، وفهرس إجابة صحيح وبقية حقول عقد الاختبار.
- تبقى بوابة الجودة المحلية مسؤولة عن التحقق التربوي: تنوع المفاهيم، prerequisite، misconception، application/transfer، وعدم ظهور placeholders أو أسئلة self-report.
- لا يتغير سلوك Teacher Studio العام؛ structured output يُفعّل فقط عندما تطلبه مهمة منظمة مثل course pre-test.

## النتيجة المتوقعة
بدل نجاح اتصال Groq ثم فشل الاستخراج بـ `question_count=0`، يعود المزود بكائن JSON مطابق للمخطط ويمكن للمنصة تقييم جودة الأسئلة فعليًا.

# V6.11.2 — إصلاح أيقونة خارطة مراحل الإنتاج

## المشكلة

بعد اكتمال أول مرحلة، كانت صفحة **نظرة عامة** تتوقف برسالة `StreamlitAPIException` عند رسم بطاقة المرحلة المكتملة.

## السبب

استُخدمت العلامة النصية `✓` في وسيط `icon` الخاص بـ `st.success`. هذا الوسيط يقبل Emoji صالحًا أو رمز Material، وليس علامة نصية عادية.

## الإصلاح

تم استبدال:

```python
st.success(copy["ready"], icon="✓")
```

بـ:

```python
st.success(copy["ready"], icon="✅")
```

لا يتطلب الإصلاح أي تعديل لقاعدة البيانات أو الأسرار أو المخرجات المحفوظة.

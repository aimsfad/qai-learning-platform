# 3alimnIA V4.7.1 - إصلاح بروتوكول الدراسة

يعالج هذا الإصدار خطأ `AttributeError` الذي ظهر عند فتح صفحة **بروتوكول الدراسة** في فضاء المقيّم.

## سبب الخطأ

كانت الصفحة تستدعي الاسمين القديمين:

- `content.PRE_TEST_QUESTIONS`
- `content.POST_TEST_QUESTIONS`

بينما يعرّف ملف المحتوى الحالي القائمتين باسم:

- `content.PRE_TEST`
- `content.POST_TEST`

## الإصلاحات المنفذة

- إضافة دالة توافق آمنة تقرأ الاسم الحالي، مع دعم الاسم القديم عند وجوده.
- منع انهيار صفحة بروتوكول الدراسة عند تغير تسمية مجموعات الأسئلة مستقبلًا.
- توطين الصفحة كاملة بالعربية والفرنسية والإنجليزية.
- ترجمة بطاقات المؤشرات، إعدادات الدراسة، ضوابط سير العمل، جدول المراجعة، وزر التنزيل.
- إبقاء أسماء أوراق ملف Excel ثابتة بالإنجليزية حتى لا تتعطل سكربتات التحليل البحثي.
- عدم تعديل قاعدة البيانات أو محتوى الاختبارات أو بيانات المشاركين.

## التحقق

- اجتاز `main_app.py` فحص `py_compile`.
- لا توجد مراجع إلى خصائص غير معرفة داخل `content.py`.
- تم التحقق من وجود 18 سؤالًا قبليًا و18 سؤالًا بعديًا و6 وحدات تعليمية.

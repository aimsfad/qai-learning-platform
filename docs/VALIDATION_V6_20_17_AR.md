# التحقق من V6.20.17

تم التحقق من:

- سلامة AST لملفات الواجهة وقاعدة البيانات.
- نجاح `compileall` لجميع ملفات Python.
- وجود استعلام المقررات المنشورة داخل الواجهة العامة الفعلية `ui_v6.py`.
- فلترة المشاريع غير الجاهزة للتشغيل قبل عرضها للعامة.
- ظهور المقررات المنشورة في الصفحة الرئيسية وصفحة البرامج.
- مزامنة بطاقات ML/AI تلقائيًا عند نشر مقرر يطابق المجال فعلًا.
- عدم تصنيف مقرر Python على أنه Machine Learning بصورة خاطئة.
- حفظ المقرر المختار عبر تسجيل الدخول/إنشاء الحساب ثم فتحه في `Published Courses`.
- عدم تضمين `secrets.toml` أو ملفات `.env` أو `.git` أو قواعد البيانات في حزمة الإصدار.

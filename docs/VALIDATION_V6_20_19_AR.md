# التحقق من V6.20.19

- Python compileall: يجب أن يمر.
- قاعدة البيانات: إنشاء `published_course_pretest_attempts` مع مفتاح فريد حسب المتعلم + المشروع + إصدار المخطط.
- التسجيل: فتح المقرر لا ينشئ Enrollment صامتًا؛ يجب الضغط على التسجيل في المقرر.
- الاختبار القبلي: كل مقرر/إصدار له محاولة مستقلة.
- العزل: الاختبار القبلي العام للكوانتوم لا يفتح مقرر الأستاذ.
- الرجوع: المتعلم المسجل سابقًا من V6.20.18 ولم ينجز Baseline يُوجَّه إلى Baseline قبل الدروس.
- CSS: Page Header orb لا يغطي العنوان.
- أسرار: لا تتضمن الحزمة `.streamlit/secrets.toml` أو `.env` أو قواعد بيانات محلية.

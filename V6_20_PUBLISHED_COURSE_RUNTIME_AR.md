# 3alimnIA V6.20.0 — تشغيل مقررات الأساتذة للمتعلمين

## الهدف

ينقل هذا الإصدار منصة 3alimnIA من مرحلة «إنتاج محتوى ثم عرض معاينة منشورة» إلى مرحلة تشغيل المقرر فعليًا للمتعلم. قبل V6.20 كان الأستاذ يستطيع بناء مخطط ودروس منظمة ومعتمدة، لكن مسار `Published Courses` كان يعرض أساسًا مخرجات المراحل القديمة في وضع قراءة فقط. V6.20 يربط النشر مباشرة بالمخطط المعتمد وأجزاء الدرس المعتمدة، ويحفظ تقدم المتعلم، ويطبق محاولة مستقلة قبل دعم الذكاء الاصطناعي، ويضيف مدربًا عامًا غير مقيد بمجال Qiskit.

## ما تغير فعليًا

### 1. بوابة نشر مرتبطة بالمحتوى المعتمد

أضيفت الدالة `teacher_project_runtime_readiness()` إلى طبقة البيانات. لا يعتبر المشروع جاهزًا للنشر في المسار الجديد إلا إذا:

- وُجد مخطط مقرر معتمد من الأستاذ.
- احتوى المخطط على دروس صالحة.
- وُجدت لكل درس أجزاء معتمدة تغطي تسلسل الدرس المصرح به في المخطط.

بهذا لم يعد اكتمال مخرج المرحلة القديمة رقم 3 وحده كافيًا للنشر عندما يكون `ENABLE_PUBLISHED_COURSE_RUNTIME=true`.

### 2. تشغيل المقرر بدل المعاينة فقط

أضيف `published_course_runtime.py` كطبقة مستقلة لرحلة المتعلم في المقررات المنشورة. وهي:

- تعرض قائمة مقررات الأساتذة المنشورة.
- تفرق بين مقرر جاهز للتعلم ومقرر قديم متاح كمعاينة فقط.
- تستخدم المخطط المعتمد وأجزاء الدرس المعتمدة فقط.
- تحفظ موضع المتعلم وتقدمه بين الجلسات.
- تمنع فتح الدروس المستقبلية قبل الوصول إليها في التسلسل.
- تسمح بالرجوع إلى الدروس السابقة بعد فتحها.
- تعرض نواتج التعلم والمحتوى المنظم لكل درس.

### 3. تثبيت نسخة المقرر عند بدء التعلم

عند أول دخول للمتعلم إلى مقرر جاهز، يسجل النظام `blueprint_run_id` داخل التسجيل. هذا يثبت المتعلم على النسخة التي بدأ بها، حتى إذا أنشأ الأستاذ لاحقًا نسخة جديدة من المخطط. لا تتم ترقية المتعلم إلى إصدار أحدث بصمت.

### 4. محاولة مستقلة قبل المدرب الذكي

حافظ الإصدار على مبدأ Attempt-First المستخدم في المنصة:

- يكتب المتعلم محاولة ذات معنى أولًا.
- تتحقق المنصة من الحد الأدنى للمحاولة باستخدام `attempt_gate`.
- بعدها فقط يفتح الدعم التكيفي.
- تحفظ المحاولة والتأمل كأدلة تشغيلية داخل المقرر، لا كحكم آلي نهائي على الإتقان.

### 5. مدرب ذكاء اصطناعي عام للمقررات

كان عقد المدرب الأصلي في `feedback_engine.py` خاصًا بدراسة Qiskit، ولذلك كان تعميمه على الرياضيات أو اللغات أو العلوم قد يؤدي إلى تعليمات غير مناسبة للمجال. أضيف مسار منفصل:

- `course_system_prompt()`
- `build_course_prompt()`
- `local_course_fallback()`
- `generate_course_tutor_response()`

يحافظ المسار الجديد على دراسة Qiskit الأصلية دون تغيير عقدها، بينما يستخدم المقرر المنشور مدربًا محايد المجال، مقيدًا بالمحتوى الذي اعتمده الأستاذ، ويطلب من النموذج التصريح بنقص السياق بدل اختراع معلومات.

### 6. دعم تكيفي محافظ وقابل للتدقيق

يستخدم المقرر المنشور `learner_model_engine` و`adaptive_support_engine` الموجودين في V6.19.1، لكن بصورة محافظة:

- لا يدعي قياس ذكاء أو قدرة ثابتة.
- لا يصدر حكم إتقان نهائي.
- يختار مستوى الدعم من المحاولة والتأمل وسجل المساعدة المتاح.
- يسجل مستوى الدعم ونمطه وثقة تغطية الأدلة وسببه.

### 7. جداول تشغيلية جديدة

أضيفت ثلاثة جداول:

- `published_course_enrollments`
- `published_course_lesson_progress`
- `published_course_ai_interactions`

هذه البيانات منفصلة حاليًا عن جداول دراسة Qiskit البحثية. الهدف هو عدم خلط استعمال المقررات العامة تلقائيًا مع بيانات البحث المضبوط أو تصديرها على أنها جزء من الدراسة دون سياسة موافقة واضحة.

### 8. إصلاح التوجيه داخل واجهة المتعلم

كان `Published Courses` مسموحًا به في منطق الصلاحيات وموجودًا في الواجهة، لكنه لم يكن مسجلًا داخل `st.navigation` في `app.py`. أضيف المسار فعليًا إلى مجموعة التعلم وإلى قائمة الأدوات الثانوية، فأصبح المسار متسقًا بين الصلاحيات والواجهة والموجه الأصلي.

### 9. لوحة نشر أفضل للأستاذ

تعرض صفحة النشر الآن:

- عدد الدروس الجاهزة من إجمالي الدروس.
- النواقص التي تمنع النشر.
- مؤشرات تشغيل مجمعة بعد النشر: التسجيلات، إكمال المقرر، إكمال الدروس، وتفاعلات المدرب الذكي.
- معاينة المخرجات القديمة داخل قسم منفصل للتدقيق بدل اعتبارها المقرر المنشور نفسه.

ولا تعرض لوحة الأستاذ نصوص محادثات المتعلمين الخام في هذه المرحلة.

### 10. تنظيف وأمان حزمة التسليم

اكتشف التدقيق أن الملف المرفوع احتوى على `.streamlit/secrets.toml` رغم أنه موجود في `.gitignore`. لذلك:

- أزيل ملف الأسرار المحلي من نسخة V6.20 المسلمة.
- أضيف `package_release.py` لبناء ZIP معقم يستبعد الأسرار و`.git` وقاعدة البيانات المحلية وذاكرة Python والملفات المؤقتة.
- يبقى `.streamlit/secrets_example.toml` فقط كقالب إعداد.
- يجب تدوير أي كلمة مرور أو رمز وصول حقيقي سبق وضعه في الحزمة القديمة، لأن مجرد حذف الملف من الإصدار الجديد لا يبطل الاعتماد القديم.

## التحقق

أضيف `validate_v620_published_course_runtime.py` ويختبر فعليًا على SQLite مؤقتة:

- إنشاء مشروع ومخطط معتمد.
- اعتماد أجزاء الدروس.
- بوابة جاهزية النشر.
- نشر المشروع.
- تثبيت نسخة المقرر عند التسجيل.
- حفظ المحاولة والتأمل والتقدم.
- تسجيل تفاعل المدرب الذكي.
- إكمال المقرر وعدم إعادة فتحه ضمنيًا عند تغيير موضع العرض.
- مؤشرات التسليم المجمعة.
- عقود التوجيه والتغليف الآمن بصورة ثابتة.

كما أضيف `validate_current_release.py` ليشغل مجموعة الانحدار المدعومة حاليًا. نتيجة الإصدار: **19/19 اختبارات إصدار + compileall ناجحة**.

## حدود هذا الإصدار

لا يدعي V6.20 أنه أنهى بنية منصة إنتاج واسعة النطاق. ما يزال العمل التالي مهمًا:

- نقل التقييمات العامة من نصوص الدرس إلى نماذج تقييم منظمة قابلة للتصحيح والتفسير.
- إدخال نظام migrations رسمي بدل الاعتماد طويل المدى على `ensure_column` وDDL داخل `init_db`.
- تفكيك `main_app.py` و`teacher_studio.py` و`db.py` تدريجيًا إلى خدمات ومستودعات وصفحات فعلية.
- إضافة اختبارات واجهة E2E على متصفح حقيقي لمسارات الأستاذ والمتعلم والمقيّم.
- تعريف سياسة صريحة للموافقة والاحتفاظ بالبيانات إذا أريد استخدام بيانات المقررات المنشورة في البحث.
- إضافة observability وإدارة أخطاء ومقاييس تشغيل أكثر منهجية قبل نشر واسع متعدد المؤسسات.

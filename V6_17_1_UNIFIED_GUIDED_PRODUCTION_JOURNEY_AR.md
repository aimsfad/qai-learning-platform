# 3alimnIA V6.17.1 — رحلة إنتاج موحّدة وموجّهة للأستاذ

## 1. سبب التحديث

أظهرت الاختبارات الحية أن واجهة الأستاذ كانت تعرض مسارين متداخلين:

1. رحلة واضحة من سبع مراحل تخص الأستاذ.
2. إحدى عشرة مرحلة تقنية قديمة تخص محركات التوليد الداخلية.

كان هذا التداخل يؤدي إلى ظهور مرحلتي «المراجع» و«الأدلة» بالحالة البرتقالية في الوقت نفسه، كما كانت حزمة الأدلة تظهر أحيانًا بعنوان مرحلة تقنية مثل `Visual asset production plan`. النتيجة كانت واجهة جميلة بصريًا، لكنها لا تجيب بوضوح عن ثلاثة أسئلة أساسية:

- أين أنا الآن؟
- ما الذي يجب اعتماده قبل المتابعة؟
- ما المرحلة التالية؟

## 2. القرار المعماري

أصبحت رحلة الأستاذ الوحيدة هي:

```text
1. إعداد المشروع
2. المراجع والبحث
3. تركيب الأدلة
4. مخطط المقرر
5. بناء الدروس
6. المراجعة والجودة
7. المعاينة والنشر
```

أما المراحل التقنية الإحدى عشرة فتبقى موجودة لدعم التوليد الخلفي وإنتاج الأصول المتخصصة، لكنها انتقلت إلى سجل متقدم مغلق ولا تُعرض بوصفها رحلة الأستاذ.

## 3. قاعدة الانتقال الجديدة

لا تُفتح أي مرحلة إلا بعد اعتماد نتيجة المرحلة السابقة:

```text
إعداد المشروع مكتمل
        ↓
تشغيل البحث ومراجعة المصادر
        ↓
اعتماد حزمة البحث الأساسية
        ↓
إنشاء حزمة الأدلة ومراجعتها
        ↓
اعتماد الأدلة
        ↓
إنشاء مخطط المقرر واعتماده
        ↓
بناء أجزاء الدروس واعتمادها
        ↓
بوابة الجودة
        ↓
المعاينة والنشر
```

وبذلك لا يمكن أن تظهر مرحلتان نشطتان في الوقت نفسه.

## 4. حزمة البحث الأساسية

أصبح البحث الموجّه في رحلة الأستاذ مرتبطًا دائمًا بالحزمة الأساسية للمقرر، تقنيًا `phase_number = 1`.

لم يعد رقم `current_phase` القديم يغيّر حزمة البحث أو حزمة الأدلة المعروضة للأستاذ. هذا يمنع ظهور عناوين غير منطقية مثل:

```text
أحدث حزمة أدلة — 4. Visual asset production plan
```

ويستبدلها بعنوان واضح:

```text
أحدث حزمة أدلة — الحزمة الأساسية للمقرر
```

## 5. اعتماد حزمة البحث

أضيفت حقول خفيفة إلى جدول `teacher_research_runs`:

```text
approved_by_teacher
approved_at
approved_by
```

كما أضيفت الدوال:

```python
latest_approved_teacher_research(...)
approve_teacher_research_run(...)
```

بعد انتهاء البحث، يراجع الأستاذ المصادر ثم يضغط:

```text
اعتماد حزمة البحث والمتابعة
```

عندها فقط تُفتح مرحلة تركيب الأدلة.

## 6. الانتقال التلقائي بعد الاعتماد

أصبح الانتقال سلسًا مع الحفاظ على Human-in-the-Loop:

- اعتماد البحث ← فتح تركيب الأدلة.
- اعتماد الأدلة ← فتح مخطط المقرر.
- اعتماد المخطط ← فتح بناء الدروس.

التوليد آلي، لكن الاعتماد يبقى قرارًا بشريًا.

## 7. بطاقة توضيح المرحلة الحالية

تحت شريط المراحل تظهر الآن ثلاث معلومات ثابتة:

```text
الآن
المراجع والبحث

تكتمل المرحلة عندما
تُراجع حزمة البحث ويعتمدها الأستاذ

بعدها
تركيب الأدلة
```

هذه البطاقة تمنع ضياع الأستاذ بين الأزرار والصفحات.

## 8. السجل التقني المتقدم

أُعيدت تسمية خريطة المراحل الإحدى عشرة إلى:

```text
سجل التوليد التقني المتقدم
```

وتظهر داخل قسم مغلق مع تنبيه واضح بأنها وظائف داخلية وليست خطوات رحلة الأستاذ.

## 9. الملفات المعدلة

```text
db.py
guided_teacher_workflow.py
teacher_studio.py
.streamlit/v6_theme.css
README.md
CHANGELOG.md
```

وأضيف:

```text
validate_v6171_unified_guided_production_journey.py
V6_17_1_UNIFIED_GUIDED_PRODUCTION_JOURNEY_AR.md
```

## 10. الترحيل وقاعدة البيانات

الترحيل تلقائي عبر `ensure_column`. لا يلزم تنفيذ SQL يدويًا، ولا تُحذف أي حزمة بحث أو أدلة قديمة.

الحزم التقنية الخاصة بالمراحل الأخرى تبقى محفوظة ويمكن الوصول إليها من السجل المتقدم.

## 11. الاختبارات المنفذة

نجحت الاختبارات التالية:

```text
V6.17.1 unified guided production journey
V6.17 hybrid background production
V6.16.5 UI stability
V6.16.4 research export and analytics
V6.16.1 guided workflow compatibility
V6.16 lesson block generation
V6.15 blueprint versioning
V6.14 lesson blueprint
V6.13 evidence synthesis
Python compileall
```

كما تم اختبار ترحيل قاعدة SQLite واعتماد حزمة البحث باستخدام قاعدة مؤقتة.

## 12. الاختبار الحي بعد النشر

1. افتح مشروع «تعلّم بايثون».
2. يجب أن تكون مرحلة واحدة فقط نشطة.
3. في «المراجع والبحث» شغّل البحث الأساسي.
4. راجع النتائج واضغط «اعتماد حزمة البحث والمتابعة».
5. يجب أن تنتقل الواجهة إلى «تركيب الأدلة» تلقائيًا.
6. يجب أن يظهر عنوان «الحزمة الأساسية للمقرر» دون اسم مرحلة تقنية.
7. بعد اعتماد الأدلة يجب الانتقال إلى «مخطط المقرر».
8. بعد اعتماد المخطط يجب الانتقال إلى «بناء الدروس».

## 13. النتيجة المتوقعة

تحوّلت الواجهة من عرض وظائف متجاورة إلى مسار إنتاج واضح ومقفل بالاعتمادات:

```text
مرحلة واحدة نشطة
إجراء رئيسي واحد
شرط اكتمال واضح
انتقال تلقائي بعد الاعتماد
سجل تقني منفصل
```

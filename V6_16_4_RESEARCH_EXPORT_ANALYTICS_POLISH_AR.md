# 3alimnIA V6.16.4 — مركز التصدير البحثي وتحسين التحليلات

## 1. هدف الإصدار

يركز هذا الإصدار على جعل مساحة المقيّم/الباحث أكثر قابلية للاستخدام في الدراسات العلمية الفعلية، مع فصل واضح بين:

- التصدير البحثي الصارم الخالي من المعرّفات المباشرة والنصوص الحرة عالية المخاطر.
- التصدير المتخصص لجدول واحد بصيغة CSV.
- حزمة إعادة إنتاج قابلة للمشاركة داخل فريق البحث.
- النسخة الإدارية الكاملة المحمية.
- سجل عمليات التصدير.

كما يحسن قراءة نشاط المدرّب التوليدي ويعيد تصميم بطاقات مراحل إنتاج المقرر.

## 2. التحسينات المنفذة

### 2.1 التبديل بين النشاط اليومي والأسبوعي

أضيف إلى رسم نشاط المدرّب التوليدي خياران:

- الدقة الزمنية: يومي أو أسبوعي.
- طريقة العرض: منحنى أو أعمدة مجمعة.

يسمح العرض الأسبوعي بقراءة الاتجاه العام عندما تكون التفاعلات اليومية متذبذبة بسبب الاختبارات أو فترات تسليم المهام.

### 2.2 تصدير بحثي صارم

أصبح التصدير البحثي الافتراضي يحذف:

- الاسم الكامل.
- البريد الإلكتروني.
- المؤسسة.
- المعرّفات الداخلية للطالب.
- اسم المقيّم.
- المطالبات والاستجابات الخام.
- التأملات والمحاولات النصية.
- التعليقات المفتوحة وسجلات الأحداث الخام التي قد تحتوي على معلومات تعريفية.

يبقى `participant_code` متاحًا لربط الجداول. لذلك فالملف **مشفّر الهوية/Pseudonymous** وليس مجهولًا بشكل غير قابل للعكس ما دام جدول الربط محفوظًا في النظام. يجب إبقاء جدول الربط خارج ملفات التحليل وتقييد الوصول إليه.

### 2.3 مرشحات موحّدة قبل التصدير

يمكن تحديد:

- مجموعة الدراسة.
- المستوى الأكاديمي.
- الحالات المكتملة أو غير المكتملة.
- فترة زمنية اختيارية.

تطبّق المرشحات على جميع الجداول التي تحتوي على رمز المشارك أو تاريخ إنشاء.

### 2.4 مصنف Excel منسق

المصنف الجديد يتضمن:

- ورقة README للنسخة والإعدادات والمرشحات.
- تجميد الصف العلوي.
- مرشح تلقائي لكل ورقة.
- رؤوس موحدة.
- عرض أعمدة قابل للقراءة.
- معالجة القيم الطويلة والمعقدة قبل كتابتها إلى Excel.

### 2.5 حزمة إعادة الإنتاج ZIP

تحتوي الحزمة على:

```text
research_export_*.xlsx
csv/*.csv
dataset_inventory.csv
data_dictionary.csv
codebook.md
analysis_template.py
README.txt
manifest.json
SHA256SUMS.txt
```

تسمح هذه البنية ببدء التحليل مباشرة عبر Pandas أو R أو SPSS/JASP بعد الاستيراد.

### 2.6 قاموس بيانات وجرد الجداول

يولد النظام تلقائيًا:

- عدد الصفوف والأعمدة والمشاركين لكل جدول.
- أقدم وأحدث تاريخ.
- وجود معرّفات مباشرة أو نصوص حرة عالية المخاطر.
- نوع كل متغير.
- عدد القيم المفقودة والفريدة.
- وصف مختصر للمتغير.
- تصنيف الحساسية.

### 2.7 CSV متخصص

يمكن اختيار أي جدول من الجداول المحضرة وتنزيله وحده بصيغة CSV بترميز UTF-8 مع BOM، مناسب لـExcel وPandas وSPSS.

### 2.8 النسخة الإدارية المحمية

لا يصبح زر النسخة الكاملة متاحًا إلا بعد تأكيد صريح من المقيّم. هذه النسخة قد تحتوي على:

- بيانات الحسابات.
- النصوص الخام.
- المطالبات والاستجابات.
- التعليقات المفتوحة.

لا يجوز مشاركتها مع فريق التحليل أو إرفاقها بمقال علمي.

### 2.9 سجل عمليات التصدير

يسجل النظام عند تحضير الملف:

- نوع التصدير.
- المرشحات.
- عدد الجداول والصفوف.
- إصدار التطبيق.
- بصمة SHA-256.
- تاريخ التحضير.

### 2.10 بطاقات مراحل الإنتاج

أعيد تصميم بطاقات المراحل الإحدى عشرة بحيث أصبحت:

- بيضاء وخفيفة بصريًا.
- ذات رقم وعنوان واضحين.
- شارة حالة صغيرة بدل كتلة خضراء كبيرة.
- حالات منفصلة: جاهز، يحتاج مراجعة، جارٍ التوليد، فشل، لم يبدأ.
- حركة Hover خفيفة دون تشتيت.

## 3. الملفات المضافة أو المعدلة

```text
research_export_center.py          جديد
main_app.py                        معدل
db.py                              معدل
teacher_studio.py                  معدل
.streamlit/v6_theme.css            معدل
validate_v6164_research_export_analytics.py  جديد
README.md                          معدل
CHANGELOG.md                       معدل
```

## 4. الاختبارات

تم تنفيذ:

```bash
python validate_v6164_research_export_analytics.py
python validate_v61631_frontend_runtime_hotfix.py
python validate_v6163_professional_layout_polish.py
python validate_v62_evaluator_dashboard.py
python -m compileall .
```

نجحت الاختبارات الثابتة وإنشاء ملفات Excel وZIP وقاموس البيانات وتطبيق المرشحات. لم ينفذ اختبار Streamlit حي في بيئة الإنشاء بسبب عدم توفر حزمة Streamlit وخدمة قاعدة البيانات الحية في هذه البيئة؛ يجب إجراء اختبار واجهة بعد الدمج في Streamlit Cloud.

## 5. اختبار النشر الحي

بعد الدمج وإعادة تشغيل التطبيق:

1. افتح فضاء المقيّم.
2. افتح لوحة التحليلات.
3. بدّل نشاط المدرّب بين اليومي والأسبوعي وبين المنحنى والأعمدة.
4. افتح مركز التصدير.
5. طبّق مرشحًا على مجموعة أو مستوى.
6. حضّر الحزمة البحثية.
7. نزّل Excel وZIP وCSV.
8. افتح `manifest.json` و`SHA256SUMS.txt` داخل ZIP.
9. تأكد من عدم وجود أسماء أو بريد أو نصوص خام في الملف البحثي.
10. اختبر النسخة الإدارية بعد تفعيل التأكيد فقط.
11. افتح سجل عمليات التصدير.
12. راجع خريطة مراحل الإنتاج في مشروع الأستاذ.

## 6. حدود الإصدار

- لا يضمن حذف النصوص الحرة وحده إخفاء الهوية غير القابل للعكس؛ رمز المشارك يظل مفتاح ربط مستعارًا.
- بصمة SHA-256 تثبت سلامة الملف ولا تشفر محتواه.
- لا تشمل الحزمة سكربتًا كاملًا للتحليل الإحصائي النهائي؛ توفر قالب بداية فقط.
- لا ينفذ الإصدار جدولة تلقائية أو تصديرًا دوريًا في الخلفية.

## 7. تطبيق التحديث عبر CMD

بعد تنزيل ملف PATCH:

```cmd
set "PROJECT=C:\Users\djenb\Downloads\Qantum study\qai_platform_"
set "PATCHZIP=%USERPROFILE%\Downloads\3alimnIA_V6.16.4_RESEARCH_EXPORT_ANALYTICS_POLISH_PATCH.zip"
set "PATCHDIR=%TEMP%\3alimnIA_V6164_PATCH"

cd /d "%PROJECT%"
git status
git switch main
git pull origin main
git switch -c feat/v6-16-4-research-export-analytics

if exist "%PATCHDIR%" rmdir /s /q "%PATCHDIR%"
mkdir "%PATCHDIR%"
tar -xf "%PATCHZIP%" -C "%PATCHDIR%"
xcopy "%PATCHDIR%\*" "%PROJECT%\" /E /I /Y /H

cd /d "%PROJECT%"
```

شغّل الاختبارات:

```cmd
py -3 validate_v6164_research_export_analytics.py
py -3 validate_v61631_frontend_runtime_hotfix.py
py -3 validate_v6163_professional_layout_polish.py
py -3 validate_v62_evaluator_dashboard.py
py -3 -m compileall .
```

ثم ارفع الملفات المحددة فقط:

```cmd
git add research_export_center.py
git add main_app.py db.py teacher_studio.py
git add .streamlit\v6_theme.css
git add validate_v6164_research_export_analytics.py
git add V6_16_4_RESEARCH_EXPORT_ANALYTICS_POLISH_AR.md
git add README.md CHANGELOG.md

git status
git diff --cached --stat

git commit -m "feat: add V6.16.4 research export and analytics polish"
git push -u origin feat/v6-16-4-research-export-analytics
```

أنشئ Pull Request إلى `main`، ثم أعد تشغيل تطبيق Streamlit ونفّذ اختبار النشر الحي الوارد أعلاه.

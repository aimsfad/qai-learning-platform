# 3alimnIA V6.13.1 — إصلاح مرونة الحصص وحدود مزودي النماذج

## 1. سبب الإصدار

ظهر أثناء تشغيل البحث وتركيب الأدلة خطآن متزامنان:

- `Gemini HTTP 429`: نفاد الحصة الحالية أو بلوغ حد الاستعمال.
- `Groq HTTP 413`: تجاوز نافذة الرموز أو حد الرموز في الدقيقة للمشروع/المؤسسة.

كان السلوك السابق يعرض الرسائل الخام للأستاذ، ويحفظ محاولة البحث الفاشلة بوصفها أحدث محاولة، ما قد يحجب حزمة بحث ناجحة سابقة.

## 2. الهدف

يهدف الإصدار إلى جعل سير العمل مقاومًا لتعطل المزود، دون اختلاق بحث ويب ودون فقدان الأدلة المخزنة.

## 3. التغييرات المنفذة

### 3.1 تصنيف أخطاء المزود

يفرق النظام الآن بين:

- نفاد الحصة الصلب: لا يعاد الطلب فورًا لأنه لن ينجح غالبًا.
- حد سرعة مؤقت: يسمح بمحاولة محدودة مع تأخير.
- تجاوز حجم/نافذة الرموز: ينتقل إلى طلب أصغر.
- أخطاء المصادقة والمهلة.

تعرض الواجهة رسائل مختصرة ولا تعرض روابط الفوترة أو معرّفات المؤسسة.

### 3.2 ضبط بحث Groq

قبل إرسال البحث إلى Groq، تضغط المنصة البرومبت ضمن ميزانية إدخال محددة وتضع حدًا لمخرجات البحث. عند ظهور 413 في الوضع المتوازن أو الموسع، تنفذ محاولة واحدة فقط باستخدام `groq/compound-mini` وبرومبت أشد اختصارًا.

### 3.3 حماية آخر حزمة بحث ناجحة

أضيف فصل بين:

- أحدث محاولة بحث، حتى لو كانت فاشلة.
- أحدث حزمة قابلة للاستعمال بحالة `completed` أو `needs_review`.

عند فشل تحديث البحث، تبقى آخر حزمة ناجحة فعالة، وتظهر رسالة تحذير بدل إيقاف المشروع.

### 3.4 بديل حتمي لتركيب الأدلة

إذا تعذر استدعاء جميع نماذج الكتابة، يستطيع V6.13.1 إنشاء بطاقات أدلة ومفاهيم أولية من سجل المصادر المخزن. تحفظ النتيجة بالحالة `needs_review`، ولا تتجاوز موافقة الأستاذ. هذا البديل لا يدعي تنفيذ بحث جديد؛ بل يعالج فقط المصادر الموجودة مسبقًا.

## 4. الإعدادات الجديدة

```toml
CONTENT_GROQ_RESEARCH_INPUT_TOKENS = "1600"
CONTENT_GROQ_RESEARCH_MAX_OUTPUT_TOKENS = "1800"
CONTENT_GROQ_RESEARCH_STRICT_INPUT_TOKENS = "850"
CONTENT_GROQ_RESEARCH_STRICT_OUTPUT_TOKENS = "900"
```

يمكن خفض القيم أكثر في الحسابات محدودة الحصة.

## 5. السلوك المتوقع

### عند وجود حزمة بحث سابقة

```text
فشل تحديث Gemini وGroq
→ حفظ محاولة الفشل للتشخيص
→ الإبقاء على آخر حزمة ناجحة
→ عرض تحذير غير معطل
→ السماح بتركيب الأدلة أو التوليد من الحزمة المخزنة
```

### عند عدم وجود أي حزمة سابقة

```text
فشل جميع مزودي البحث
→ عدم اختلاق مصادر أو روابط
→ عرض رسالة مبسطة
→ طلب المحاولة لاحقًا أو معالجة الحصة
```

### عند فشل نموذج تركيب الأدلة

```text
مصادر مخزنة موجودة
→ تقييم وإزالة التكرار
→ إنشاء سجلات حتمية أولية
→ الحالة needs_review
→ مراجعة الأستاذ إلزامية
```

## 6. الملفات المعدلة

- `web_research_engine.py`
- `educational_builder.py`
- `evidence_synthesis_engine.py`
- `teacher_studio.py`
- `db.py`
- `.streamlit/secrets_example.toml`
- `README.md`
- `CHANGELOG.md`
- `validate_v6131_provider_quota_resilience.py`

## 7. الاختبارات

يغطي الاختبار الجديد:

- إخفاء الرسائل الحساسة والروابط.
- ضغط برومبت البحث.
- الانتقال من Groq Compound إلى Compound Mini بعد 413.
- عدم إخفاء البحث الناجح بمحاولة فاشلة لاحقة.
- استعمال الحزمة المخزنة عند فشل التحديث.
- إنشاء بديل حتمي لتركيب الأدلة عند تعطل النماذج.

## 8. القيود

لا يمكن للكود زيادة حصة Gemini أو Groq. معالجة 429 الدائم تتطلب مراجعة لوحة الاستخدام والفوترة أو انتظار تجدد الحصة. كما أن البحث الحقيقي لا يمكن تعويضه محليًا من دون مصدر ويب متاح؛ لذلك يحافظ النظام على النزاهة ويمنع اختلاق المراجع.

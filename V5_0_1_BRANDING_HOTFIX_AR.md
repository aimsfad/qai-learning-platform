# 3alimnIA V5.0.1 - إصلاح تشغيل الهوية

## سبب الخطأ

كان شريط الأدوات يستعمل `branding.BRAND_NAME_LATIN`، بينما ملف `branding.py` يعرّف الاسم تحت `BRAND_NAME`. كما أن الوسيط الافتراضي في `dict.get` يتم تقييمه حتى عندما يكون الدور موجودًا، لذلك توقف التطبيق عند التشغيل.

## الإصلاح

- استعمال fallback آمن في `app.py`.
- إضافة alias متوافق مع الإصدارات السابقة في `branding.py`.
- إضافة فحص ساكن لكل مراجع `branding.*` لمنع تكرار المشكلة.

لا توجد تغييرات على قاعدة البيانات أو المحتوى أو المسارات التعليمية.

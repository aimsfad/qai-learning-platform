# ترقية 3alimnIA من V6.19.1 إلى V6.20.0

## قبل البدء

لا تستبدل مجلد التشغيل الحالي مباشرة. احتفظ بنسخة احتياطية محلية من قاعدة البيانات وملف الإعدادات السرية، ثم اختبر V6.20 في مجلد جديد.

## 1. فك حزمة V6.20

الحزمة المسلمة لا تحتوي عمدًا على:

- `.streamlit/secrets.toml`
- `data/`
- `.git/`
- ملفات cache وملفات المحرر المؤقتة

وهذا مقصود لمنع تسريب الاعتمادات أو بيانات المستخدمين.

## 2. استعادة قاعدة البيانات محليًا عند الحاجة

إذا كنت تريد متابعة نفس حسابات ومشاريع وبيانات النسخة السابقة، انسخ مجلد `data/` من نسختك المحلية الموثوقة إلى مجلد V6.20 الجديد. لا ترسل قاعدة البيانات ضمن ZIP عام.

عند أول تشغيل، `db.init_db()` ينشئ جداول V6.20 الإضافية تلقائيًا دون حذف الجداول السابقة.

## 3. إعداد Secrets

استخدم `.streamlit/secrets_example.toml` كمرجع. أنشئ محليًا `.streamlit/secrets.toml` أو استخدم متغيرات البيئة المناسبة.

مهم: لأن الحزمة القديمة التي رُفعت للمراجعة كانت تحتوي ملف Secrets محليًا، غيّر أي كلمة مرور أو رمز وصول أو مفتاح حقيقي كان موجودًا فيها قبل إعادة الاستخدام. لا تكتفِ بنسخ القيم القديمة كما هي.

فعّل المسار الجديد:

```toml
ENABLE_PUBLISHED_COURSE_RUNTIME = "true"
```

## 4. تثبيت المتطلبات

```bash
python -m pip install -r requirements.txt
```

## 5. التحقق قبل التشغيل

```bash
python validate_current_release.py
```

النتيجة المتوقعة لهذا الإصدار:

```text
Current release validation passed: 19/19 validators + compileall.
```

## 6. تشغيل المنصة

```bash
streamlit run app.py
```

## 7. سيناريو اختبار V6.20

1. سجّل الدخول كأستاذ.
2. افتح مشروعًا له blueprint معتمد.
3. تأكد من اعتماد جميع أجزاء كل درس.
4. افتح مرحلة النشر وتأكد أن `الدروس الجاهزة` تساوي العدد الكامل.
5. انشر المشروع.
6. ادخل كمتعلم وافتح `دورات الأساتذة`.
7. ابدأ المقرر واكتب محاولة مستقلة في الدرس الأول.
8. احفظ المحاولة ثم اطلب دعمًا من المدرب الذكي.
9. اكتب التأمل وأكمل الدرس.
10. تحقق من عدم فتح درس مستقبلي قبل الوصول إليه.
11. عد إلى حساب الأستاذ وتحقق من مؤشرات التسجيل والإكمال واستخدام المدرب.

## ملاحظة عن الإصدارات القديمة المنشورة

المشروعات التي نُشرت قبل V6.20 ولم تملك blueprint/lesson-block package معتمدًا ستظل قابلة للعرض بصيغة legacy preview، لكنها لن تتحول تلقائيًا إلى مقرر تفاعلي. يجب إكمال واعتماد خط الإنتاج المنظم ثم إعادة النشر وفق بوابة V6.20.

# 3alimnIA V6.20.21 — Published Course Entry Routing

## المشكلة

كان اختيار مقرر منشور من الأستاذ يحفظ `project_id` بصورة صحيحة، لكن واجهة الحساب بقيت مرتبطة بمسار دراسة Qiskit: نموذج إنشاء الحساب كان يطلب المعرفة السابقة في البرمجة الكمية ويعرض موافقة الدراسة، والحساب الجديد كان يعود إلى الصفحة الرئيسية للدراسة بدل تجربة المقرر العامة.

## الإصلاح

- الحفاظ على المقرر المختار عبر تسجيل الدخول أو إنشاء الحساب.
- إعادة الحساب الموجود مباشرة إلى `Published Courses` بعد المصادقة.
- إنشاء حساب عام واحد لمقررات الأساتذة عند التسجيل من مقرر منشور، مع `study_group=general_course`.
- عدم طلب مستوى Python/Quantum أو كود الدراسة أو موافقة دراسة Qiskit عند إنشاء حساب عام للمقرر.
- عدم حفظ `consent_records` الخاصة بالدراسة لمجرد الالتحاق بمقرر عام.
- منع إنشاء حساب ثانٍ إذا كان البريد موجودًا؛ يوجَّه المتعلم إلى استعمال حسابه الحالي.
- إبقاء الاختبار القبلي للمقرر داخل `published_course_pretest_attempts` منفصلًا عن `test_attempts` الخاصة بدراسة Qiskit.
- إخفاء شريط تقدم دراسة Qiskit داخل صفحة مقررات الأساتذة.
- إعطاء الحساب العام صفحة متعلم محايدة بدل إدخاله تلقائيًا في المسار الكمومي.

## ما لم يتغير

- Runtime المقررات المنشورة.
- Enrollment لكل `(student_id, project_id)`.
- تثبيت نسخة الـ blueprint عند الالتحاق.
- تقدم الدروس والتفاعلات مع المدرب الذكي.
- مسار دراسة Qiskit للحسابات المشاركة فيه أصلًا.

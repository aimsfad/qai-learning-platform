# 3alimnIA V6.11.1 — إصلاح ميزانية البرومبت وتنسيق RTL

يعالج هذا التحديث المشكلتين اللتين ظهرتا في أول اختبار حي لمحرك إنشاء المحتوى:

1. رفض Groq للطلب بالخطأ `HTTP 413` لأن مجموع رموز الإدخال والإجابة المطلوبة تجاوز حد الخطة الحالية.
2. اختلاط العربية والإنجليزية داخل العناوين، مع أحجام عناوين كبيرة وغير مستقرة في المعاينة.

## ما تغير

- أضيف تقدير محافظ لحجم الطلب قبل الاتصال بالمزوّد.
- تُحفظ النسخة الكاملة من البرومبت للتنزيل والتدقيق، بينما يتلقى Groq نسخة تشغيلية مضغوطة تحافظ على:
  - بيانات المشروع الأساسية؛
  - المراجع ومحتوى الأستاذ ضمن ميزانية مضبوطة؛
  - تعليمات المرحلة الحالية؛
  - سياق المراحل السابقة المقبولة؛
  - عقد الأدلة وعقد الاستجابة.
- الحد الافتراضي لطلب Groq أصبح قرابة 7000 رمز إجمالي، مع حد أقصى افتراضي قدره 2600 رمز للإجابة.
- عند استمرار خطأ 413، يجري النظام محاولة ثانية واحدة بضغط أشد قبل الانتقال إلى Gemini.
- أزيلت روابط المزوّد ومعرّفات المؤسسة من رسائل التشخيص المعروضة للمستخدم.
- أضيفت ميزانية الطلب داخل واجهة الإنتاج قبل الضغط على زر التوليد.
- أضيف تطبيع تلقائي لمخرجات العربية:
  - عنوان رئيسي عربي موحّد لكل مرحلة؛
  - منع خلط العربية والإنجليزية داخل العنوان نفسه؛
  - نقل المصطلح الإنجليزي إلى سطر مستقل بصيغة آمنة للاتجاه؛
  - تحويل `Generation checks` إلى `فحوص التوليد`.
- أضيف تنسيق CSS محصور في مخرجات التوليد فقط لضبط RTL، أحجام العناوين، الجداول، وكتل الكود.

## إعدادات Streamlit الموصى بها

أضيفي القيم التالية إلى إعدادات Secrets في Streamlit Cloud. لا تضعيها في مستودع Git الحقيقي:

```toml
ENABLE_PROVIDER_PROMPT_BUDGETING = "true"
CONTENT_GROQ_TOTAL_TOKEN_BUDGET = "7000"
CONTENT_GROQ_MAX_OUTPUT_TOKENS = "2600"
CONTENT_GROQ_TOKEN_RESERVE = "350"
CONTENT_GROQ_STRICT_INPUT_TOKENS = "2200"
CONTENT_GROQ_STRICT_OUTPUT_TOKENS = "1700"
CONTENT_GEMINI_MAX_OUTPUT_TOKENS = "6000"
```

## سلوك المحرك بعد التحديث

```text
البرومبت الكامل المحفوظ
        ↓
حساب الحجم التقديري
        ↓
إن كان Groq هو المزوّد: ضغط سياقي ضمن الميزانية
        ↓
محاولة Groq العادية
        ↓ عند HTTP 413 فقط
محاولة Groq مضغوطة بشدة
        ↓ عند الفشل
Gemini أو المزوّد الاحتياطي التالي
        ↓
تطبيع لغوي واتجاهي للناتج
        ↓
التحقق والحفظ والانتقال إلى المرحلة التالية
```

## الاختبارات

```cmd
py -3 validate_v611_content_builder.py
py -3 validate_v6111_prompt_budget_rtl.py
py -3 validate_v6101_ai_tutor_state.py
py -3 validate_v610_gemini_router.py
```

لا يغيّر هذا التحديث مخطط قاعدة البيانات.

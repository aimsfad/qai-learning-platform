# 3alimnIA V6.20.5 — Mobile Header Shell

## سبب الإصدار

أظهر الاختبار المرئي على Streamlit Cloud بعرض 490px أن V6.20.4 ما زال يتأثر بقواعد التخطيط القديمة: الشعار الأفقي ظهر داخل مربع مقصوص، وزر القائمة بقي في صف مستقل بعرض الصفحة، مع فراغ رأسي كبير قبل المحتوى.

## الإصلاح

- أزيلت الأعمدة `st.columns` بالكامل من رأس الصفحة العامة على الهاتف.
- أصبح الشعار صورة HTML مستقلة ذات class خاص `v6205-mobile-logo-img`، بدل استعمال classes الهوية القديمة التي تراكمت عليها قواعد CSS عبر الإصدارات.
- ثُبّت زر القائمة داخل نفس شريط الرأس باستخدام `position:absolute`، لذلك لا يمكن أن يهبط إلى سطر ثانٍ بسبب responsive stacking في Streamlit.
- عُزلت قواعد الشعار عن `stImage` وعن `brand-approved-logo-img` لمنع القص أو التحويل إلى مربع.
- ضُبط الرأس على ارتفاع 58px فقط، مع نسخة 56px للشاشات الأصغر من 420px.
- استمر استعمال Popover الأصلي في Streamlit للقائمة حتى تبقى الأزرار قابلة للتفاعل والوصول بلوحة المفاتيح.
- لم يتغير routing أو منطق التعلم أو الحسابات أو قواعد البيانات.

## التحقق

- `validate_current_release.py`: 24/24 validators passed.
- `compileall`: passed ضمن مجموعة التحقق.
- لا توجد migration جديدة.
- حزمة الإصدار تُبنى بواسطة `package_release.py` مع استبعاد secrets وقواعد البيانات وGit/cache.

## الاختبار المطلوب بعد النشر

إعادة فتح الصفحة الرئيسية على Streamlit Cloud عند 390×844 ثم 490×844 والتأكد من أن أول viewport يعرض: شريطًا واحدًا صغيرًا يحتوي الشعار كاملًا + زر القائمة، ثم الـHero مباشرة دون مساحة فارغة كبيرة.
